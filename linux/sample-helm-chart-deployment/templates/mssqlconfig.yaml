kind: ConfigMap 
apiVersion: v1 
metadata:
  name: mssql-config
data:
  mssql.conf: |
    [EULA]
    accepteula = Y
    accepteulaml = Y
    
    [coredump]
    captureminiandfull = true
    coredumptype = full
    
    [hadr]
    hadrenabled = 1
    
    [language]
    lcid = 1033
    
    [filelocation]
    defaultdatadir = /var/opt/mssql/data/db
    defaultlogdir = /var/opt/mssql/data/log
    defaultdumpdir = /var/opt/mssql/data/dump
    defaultbackupdir = /var/opt/mssql/data/backup
